# Tachyon Core {{VERSION}}

## 发布标识

- 版本：`{{VERSION}}`
- 源代码提交：`{{COMMIT}}`
- 发布通道：alpha 预发布版

## 兼容性

- 支持 Windows、macOS 和 Linux 的 AMD64、ARM64。
- 六个目标平台 ZIP 均包含 `tachyon-core` 和 `tachyonctl`。
- Windows 的 `wintun.dll` 由 Prism 外置供应链管理，发布包不内置该 DLL。

## 校验与发布资产

安装前请下载 `SHA256SUMS.txt` 并校验完整资产集合。发布还包含确定性的
`BUILD_METADATA.json`、已核实的 `WINTUN_SIDECAR_CONTRACT.json`、
`EVIDENCE_MANIFEST.json` 以及去敏 Helper evidence 压缩包。发布 workflow 会
验证发布已变为 immutable，并核对 GitHub 资产 digest、大小与本地清单一致。

## Alpha 预览边界

- `{{VERSION}}` 是 **WFP Helper / Captured UDP Named Pipe v2 Preview（WFP Helper / 捕获 UDP Named Pipe v2 预览版）**。
- 本版本包含 Core 侧认证 Named Pipe v2 契约、Helper/Service 预览契约、租约与注册表控制、TGP 桥接、生命周期清理以及 single-writer 关闭竞态修复。
- 本预览版**不包含真实 WFP callout、不包含签名 WFP 驱动、不包含内核注入、不包含进程捕获，也没有真实游戏端到端（E2E）验证**。Helper evidence 明确处于 fail-closed 的 `capture_provider=not_ready` 状态。
- `wintun.dll` 由 Prism 根据官方 sidecar 契约管理。sidecar 缺失或 SHA-256 不匹配时，Prism 必须拒绝启动 Core。

## 限制

- Tachyon Core 仍为 alpha 软件，尚不稳定，也不完整。
- Prism 托管的系统代理接管默认关闭；Core 不会修改宿主机代理设置。
- 客户端 TUN 自动路由和 DNS hijack 尚不受支持，并会被配置校验拒绝。
- 真实 VPS、运营商/网络、客户端、目标游戏 UDP 以及具备管理员权限的 Windows TUN 仍需现场验证。
