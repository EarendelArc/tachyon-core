name: Windows WFP Driver Gate

on:
  pull_request:
    paths:
      - ".github/workflows/wfp-driver.yml"
      - "drivers/windows/wfp/**"
      - "internal/helper/**"
  push:
    branches:
      - main
      - feature/windows-wfp-dataplane
    paths:
      - ".github/workflows/wfp-driver.yml"
      - "drivers/windows/wfp/**"
      - "internal/helper/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  abi-contract:
    name: Generated ABI contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
      - name: Setup Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version-file: go.mod
          cache: true
      - name: Regenerate and require a clean tree
        run: |
          go generate ./internal/helper
          git diff --exit-code -- drivers/windows/wfp/include/tachyon_wfp_abi.h internal/helper/wfp_abi_generated.go
      - name: Run helper ownership and transaction tests
        run: go test -race ./internal/helper -count=1

  wdk-build:
    name: WDK compile and analysis (${{ matrix.platform }})
    needs: abi-contract
    runs-on: windows-2025
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: x64
          - platform: ARM64
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v3
        with:
          msbuild-architecture: x64
      - name: Restore pinned formal WDK NuGet package
        shell: pwsh
        run: msbuild drivers/windows/wfp/TachyonWfp.sln /t:Restore /p:Configuration=Release /p:Platform=${{ matrix.platform }} /p:TachyonUseNuGetWDK=true
      - name: Compile with PREfast enabled
        shell: pwsh
        run: >-
          msbuild drivers/windows/wfp/TachyonWfp.sln
          /m /t:Build
          /p:Configuration=Release
          /p:Platform=${{ matrix.platform }}
          /p:TachyonUseNuGetWDK=true
          /p:RunCodeAnalysis=true
          /p:EnablePREfast=true
          /warnaserror

  inf-verify:
    name: WDK InfVerif
    needs: wdk-build
    runs-on: windows-2025
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v3
        with:
          msbuild-architecture: x64
      - name: Restore pinned host WDK NuGet package
        shell: pwsh
        run: msbuild drivers/windows/wfp/TachyonWfp.sln /t:Restore /p:Configuration=Release /p:Platform=x64 /p:TachyonUseNuGetWDK=true
      - name: Run host WDK InfVerif and validate INF
        shell: pwsh
        run: |
          $packageRoot = Join-Path $env:USERPROFILE ".nuget\packages\microsoft.windows.wdk.x64\10.0.28000.2526"
          $infverif = Join-Path $packageRoot "c\tools\10.0.28000.0\x64\infverif.exe"
          if (-not (Test-Path -LiteralPath $infverif -PathType Leaf)) { throw "Host InfVerif was not found in pinned WDK package $packageRoot" }
          & $infverif /w drivers/windows/wfp/tachyon-wfp.inf
          if ($LASTEXITCODE -ne 0) { throw "InfVerif failed with exit code $LASTEXITCODE" }
